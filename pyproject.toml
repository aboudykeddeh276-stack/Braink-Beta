[project]
name = "braink-beta-keddeh-math"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "openpyxl>=3.1",
]

[project.optional-dependencies]
dev = [
    "pytest",
]

[tool.setuptools]
packages = ["keddeh_math"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
