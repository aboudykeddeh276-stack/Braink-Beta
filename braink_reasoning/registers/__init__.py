"""Data registers: literal lookup tables, as opposed to keddeh_math's
computed functions.

Each register here is either objective external fact (`periodic_table`,
standard IUPAC data) or transcribed verbatim from the user's own source
spreadsheet (`compounds`, `lexicon`) -- nothing in this subpackage is
generated or inferred.
"""
